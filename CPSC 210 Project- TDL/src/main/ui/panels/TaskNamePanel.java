package ui.panels;

import java.awt.*;
import javax.swing.*;

public class TaskNamePanel extends JPanel {

    //EFFECTS: make the Task Name Panel
    public TaskNamePanel() {
        setLayout(new FlowLayout());
        setBackground(new Color(141, 175, 180));
        setPreferredSize(new Dimension(1, 30));
    }
}
