package ui.panels;

import java.awt.*;
import javax.swing.*;

public class TaskNumPanel extends JPanel {

    //EFFECTS: make the Task Number/ID Panel
    public TaskNumPanel() {
        setLayout(new FlowLayout());
        setBackground(new Color(141, 175, 180));
        setPreferredSize(new Dimension(1, 30));
    }
}
