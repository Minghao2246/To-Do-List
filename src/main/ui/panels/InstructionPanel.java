package ui.panels;

import java.awt.*;
import javax.swing.*;

public class InstructionPanel extends JPanel {

    //EFFECTS: make the instruction panel
    public InstructionPanel() {
        setLayout(new FlowLayout());
        setBackground(new Color(220, 216, 216));
        setPreferredSize(new Dimension(1, 25));
    }
}
